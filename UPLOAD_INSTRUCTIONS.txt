BINANCE INSIGHT V2 — BACKTEST DROPDOWNS

Replace these complete files in your repository:

1. trading/views.py
2. templates/backtest.html

What this adds:
- Pair dropdown populated from the Strategy v2 CORE_BASES universe.
- Timeframe dropdown: 15m, 30m, 1h, 4h.
- Server-side validation so only approved pair/timeframe choices are run.
- Selected pair/timeframe remain selected after each run.
- Backtest history table now shows the latest 50 runs.
- No database migration required.
- trading/services/backtest.py does not need replacement because it already accepts interval=.

After upload/deploy:
1. Open /backtest/
2. Choose a pair.
3. Choose a timeframe.
4. Click Run backtest.
5. Repeat for other pairs/timeframes and compare the stored rows.
