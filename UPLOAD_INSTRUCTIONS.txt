BINANCE INSIGHT V2 — CORRECTED R MULTIPLE

Replace these three files in your GitHub repository with the complete files in this ZIP:

1. trading/services/paper.py
2. trading/views.py
3. templates/paper.html

No database migration is required.

WHAT CHANGED
- 1R is now the ACTUAL planned dollar loss if that specific position hits its original stop.
- It includes entry-to-stop price loss, entry fee, expected stop-exit fee, and configured stop slippage.
- It no longer uses the maximum position-sizing risk budget as the R denominator.
- New trades permanently freeze initial_risk_dollars, fee rate, slippage rate and expected stop fill in Trade.metadata.
- Existing Strategy v2 trades are reconstructed automatically from their recorded entry, stop, quantity and entry fee, so the existing INJUSDT trade should move from about -0.72R to approximately -1.00R.

After upload, Render should redeploy automatically if your service is linked to GitHub. Keep BINANCE_MODE=paper and LIVE_TRADING_ENABLED=false while proving Strategy v2.
